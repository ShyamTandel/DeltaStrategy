name: Deploy to Production

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - dev-api

env:
  IMAGE_NAME: shyamtandel/deltastrategy  # Update with your Docker Hub username
  STACK_NAME: deltastrategy

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2
        
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_TOKEN }}
      
      - name: Build and push Docker image
        uses: docker/build-push-action@v4
        with:
          context: .
          file: ./Dockerfile.prod
          push: true
          tags: ${{ env.IMAGE_NAME }}:latest,${{ env.IMAGE_NAME }}:${{ github.sha }}
          cache-from: type=registry,ref=${{ env.IMAGE_NAME }}:buildcache
          cache-to: type=registry,ref=${{ env.IMAGE_NAME }}:buildcache,mode=max
      
      - name: Build completed
        run: echo "✅ Docker image built and pushed successfully to Docker Hub!"

  # Uncomment this job when you have a production server ready
  # deploy:
  #   runs-on: ubuntu-latest
  #   needs: build-and-push
  #   steps:
  #     - name: Deploy to server via SSH
  #       uses: appleboy/ssh-action@master
  #       with:
  #         host: ${{ secrets.PROD_HOST }}
  #         username: ${{ secrets.PROD_USERNAME }}
  #         key: ${{ secrets.PROD_SSH_KEY }}
  #         script: |
  #           cd /path/to/your/app
  #           
  #           echo "Pulling latest Docker image..."
  #           docker pull ${{ env.IMAGE_NAME }}:latest
  #           
  #           echo "Deploying with docker-compose..."
  #           export IMAGE_NAME=${{ env.IMAGE_NAME }}
  #           docker-compose -f docker-compose.prod.yml pull
  #           docker-compose -f docker-compose.prod.yml up -d
  #           
  #           echo "Cleaning up unused Docker resources..."
  #           docker system prune -f
  #           
  #           echo "Deployment completed successfully!"
